A production-ready microservices architecture built with:

- FastAPI — REST APIs
- gRPC — fast service communication
- RabbitMQ — async messaging
- PostgreSQL — database per service pattern
- Redis — caching layer
- Docker + Docker Compose — containerization
- Kubernetes — orchestration (coming soon)
- CI/CD — automated deployment (coming soon)
